//Immidiately Invoked Function Expression(IIFE)

// (function chai(){
//     console.log("DB CONNECTED");
// })
// //to avoid global  scope pollution we use iife
// //where function is wrapped in () and from () we call the function
// ();

// (
//     (name)=> (console.log(`name is ${name}`))
// ) ("ujwal");